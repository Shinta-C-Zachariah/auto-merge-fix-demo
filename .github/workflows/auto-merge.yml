name: Auto Fix Merge Conflict

on:
  push:

jobs:
  auto-fix:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4

      - name: Run Auto Fix
        run: |
          python scripts/auto_fix_merge.py

      - name: Push Fix
        run: |
          git config user.name "auto-bot"
          git config user.email "bot@example.com"
          git push